# Public assets

| File | Used by |
| --- | --- |
| `profile.png` | Hero portrait (`profile.photo` in [src/data/site.ts](../src/data/site.ts)) |
| `Awais-Ahmad-Software-Engineer.pdf` | Every "Download CV" button (`profile.cv`) |

Anything placed here is served publicly at the site root, so keep unrelated
files out.
